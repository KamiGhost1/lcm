{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core webview permissions for the main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
